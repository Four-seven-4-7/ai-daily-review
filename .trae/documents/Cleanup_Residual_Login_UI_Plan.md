# 残留登录功能移除方案

经过代码审查，我发现 `index.html` 中虽然移除了 `AuthScreen` 组件，但没有移除与之相关的“邮箱显示”和“退出登录”按钮。这导致您在界面上仍然看到了和登录相关的内容。

这部分残留代码位于顶部的工具栏区域（`Space` 组件内）。

## 修复方案
1.  **清理残留代码**：
    *   移除界面上显示的 `{user.email}`。
    *   移除“退出”按钮及其绑定的 `signOut` 逻辑。
2.  **验证**：确保界面上只保留“自动保存/云端拉取/设置/导出/历史记录”这些核心功能按钮。

## 执行计划
1.  编辑 `index.html`，定位到顶部的 `<Space>` 组件。
2.  删除 `<span style={{ fontSize: 12, color: '#999' }}>{user.email}</span>`。
3.  删除 `<Button type="text" danger ...>退出</Button>`。
4.  提交更改到 Git，自动触发 Vercel 更新。

---
**是否确认执行此清理？**
（执行后，界面将彻底变回纯粹的工具形态，看不到任何账号痕迹）